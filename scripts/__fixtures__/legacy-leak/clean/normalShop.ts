// Fixture: normal Catchmon Shop-shaped source with none of the watched
// legacy terms. Used only by the guard's own test suite (negative case).
export function addCoins(balance: number, amount: number): number {
  return balance + amount;
}
