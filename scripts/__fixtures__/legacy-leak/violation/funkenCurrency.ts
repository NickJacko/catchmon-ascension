// Fixture: represents active Funken currency/domain code. Used only by
// the guard's own test suite.
export function addFunken(balance: number, amount: number): number {
  return balance + amount;
}
