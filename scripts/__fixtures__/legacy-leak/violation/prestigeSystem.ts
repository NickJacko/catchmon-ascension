// Fixture: represents an active prestige/reset gameplay implementation.
// Used only by the guard's own test suite.
export function resetForPrestige(lifetimeEarned: number): number {
  return lifetimeEarned;
}
