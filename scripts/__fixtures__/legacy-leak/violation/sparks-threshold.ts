// Fixture: represents a region/unlock definition reintroducing the old
// sparksThreshold gameplay field. Used only by the guard's own test suite.
export interface FixtureRegionUnlock {
  sparksThreshold: number;
}
